{
	"name": "new-project",
	"version": "0.1.0",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"dev": "bun run src/server.ts",
		"build": "bun build src/server.ts --target=bun --outdir dist",
		"prod": "bun --bun run dist/server.js"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@punk.sys/img-sdk": "^0.1.1",
		"@fastify/static": "^8.3.0",
		"fastify": "^5.6.2",
		"sharp": "^0.34.5"
	}
}
