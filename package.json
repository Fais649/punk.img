{
	"name": "punk-img",
	"version": "0.1.0",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "bun --workspaces run build",
		"publish": "bun run build && bun publish"
	}
}
